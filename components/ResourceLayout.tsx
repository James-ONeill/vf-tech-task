import { DetailedResource } from "@/types/API";
import { ReactNode } from "react";

interface Props {
  children: ReactNode;
  resource: DetailedResource
}

export default function ResourceLayout({ children, resource }: Props) {
  return (
    <div>
      
    </div>
  )
}